# vendor/

This platform intentionally ships with **zero third-party dependencies** — the entire
framework is implemented in `core/` and autoloaded through `Core\Foundation\Autoloader`
(PSR-4). This directory is reserved for future Composer packages if a dependency is
ever required.
